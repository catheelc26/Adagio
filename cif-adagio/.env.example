# Credenciales del proyecto de Supabase (Project Settings → API).
# Copia este archivo a .env y rellena los valores reales. No se sube a git (ver .gitignore).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
